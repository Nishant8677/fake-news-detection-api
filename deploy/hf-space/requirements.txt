# The serving stack is pinned to the environment BENCHMARK.md was measured on,
# so the hosted demo and the published latency numbers describe the same code.
fastapi==0.129.0
uvicorn==0.41.0
pydantic==2.12.5

torch==2.10.0
transformers==5.2.0

pandas==3.0.1
numpy==2.4.2

httpx==0.28.1

# The UI layer is deliberately NOT pinned to an exact version.
#
# huggingface-hub is a transitive dependency of transformers, which requires
# >=1.3.0,<2.0. Pinning it here to a version picked by hand produced a
# ResolutionImpossible; the resolver should choose it, not me. gradio is left on
# a floor for the same reason -- it pulls a large dependency tree of its own and
# over-constraining it breaks the build without making the benchmarks any more
# reproducible. Nothing in this section affects the measured serving path.
gradio>=5.9
